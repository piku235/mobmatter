import("//build_overrides/chip.gni")

source_set("sqlite") {
  sources = [
    "SqlitePersistentStorageDelegate.cpp",
  ]

  public = [
    "SqlitePersistentStorageDelegate.h",
  ]

  deps = [
    "${chip_root}/src/lib",
    "//src/common/persistence/sqlite",
  ]

  configs += ["//:includes"]
}
