import("//build_overrides/chip.gni")

source_set("logging") {
  sources = [
    "LoggerAdapter.cpp",
  ]

  public = [
    "LoggerAdapter.h",
  ]

  deps = [
    "${chip_root}/src/lib",
    "//src/common/logging",
  ]

  configs += ["//:includes"]
}
