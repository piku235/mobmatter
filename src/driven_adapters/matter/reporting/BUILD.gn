import("//build_overrides/chip.gni")

source_set("cover") {
    sources = [ "CoverReportingAdapter.cpp" ]

    public = [ "CoverReportingAdapter.h" ]

    deps = [
        "${chip_root}/src/app",
        "//src/application",
    ]

    configs += [ "//:app_includes" ]
}

source_set("switch") {
    sources = [ "SwitchReportingAdapter.cpp" ]

    public = [ "SwitchReportingAdapter.h" ]

    deps = [
        "${chip_root}/src/app",
        "//src/application",
    ]

    configs += [ "//:app_includes" ]
}
