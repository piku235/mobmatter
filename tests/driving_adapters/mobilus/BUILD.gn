source_set("device_initializer") {
  sources = [ "MqttMobilusDeviceInitializerTest.cpp" ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/common/logging",
    "//src/driving_adapters/mobilus:device_initializer",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}

source_set("device_state_syncer") {
  sources = [ "MqttMobilusDeviceStateSyncerTest.cpp" ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/common/logging",
    "//src/driving_adapters/mobilus:device_state_syncer",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}

source_set("device_name_syncer") {
  sources = [ "MqttMobilusDeviceNameSyncerTest.cpp" ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/driving_adapters/mobilus:device_name_syncer",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}

source_set("device_event_subscriber") {
  sources = [ "MqttMobilusDeviceEventSubscriberTest.cpp" ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/driving_adapters/mobilus:device_event_subscriber",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}
