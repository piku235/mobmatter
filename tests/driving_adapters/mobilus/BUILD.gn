source_set("device_initializer") {
  sources = [
    "MqttMobilusDeviceInitializerTest.cpp",
  ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/common/logging",
    "//src/driving_adapters/mobilus:device_initializer",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}

source_set("device_state_syncer") {
  sources = [
    "MqttMobilusDeviceStateSyncerTest.cpp",
  ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/common/logging",
    "//src/driving_adapters/mobilus:device_state_syncer",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}

source_set("event_subscriber") {
  sources = [
    "MqttMobilusEventSubscriberTest.cpp",
  ]

  include_dirs = [
    "//src",
    "//tests",
  ]

  deps = [
    "//src/application",
    "//src/driving_adapters/mobilus:event_subscriber",
    "//tests/mobilus:mock_client",
    "//third_party/googletest:gtest",
  ]
}
